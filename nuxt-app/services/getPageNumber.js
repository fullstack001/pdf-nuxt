// import { GlobalWorkerOptions, getDocument } from "@/node_modules/pdfjs-dist/legacy/build/pdf.mjs";
// import PDFJSWorker from "@/node_modules/pdfjs-dist/legacy/build/pdf.worker.mjs";
 

// GlobalWorkerOptions.workerSrc = PDFJSWorker;

const getPageNumber = async (file) => {
  // let url = "";
  // var num = 0;

  // if (file.link) {
  //   url = file.link;
  // } else {
  //   url = URL.createObjectURL(file);
  // }
  // await getDocument(url).promise.then(
  //   (pdf) => {
  //     num = pdf.numPages;
  //   },
  //   (reason) => {
  //     console.error(reason);
  //   }
  // );
  // console.log(num);
  // return num;
  return 0
};

export default getPageNumber;
